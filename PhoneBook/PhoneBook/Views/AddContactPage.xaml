<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="PhoneBook.Views.AddContactPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Command="{Binding CancelCommand}" />
        <ToolbarItem Text="Save" Command="{Binding SaveCommand}" />
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout Spacing="10" Padding="15">
            <Label Text="Imię" FontSize="Medium" />
            <Entry Text="{Binding FirstName}" d:Text="Jan" FontSize="Small" />
            <Label Text="Nazwisko" FontSize="Medium" />
            <Editor Text="{Binding LastName}" d:Text="Kowalski" FontSize="Small" Margin="0" />
            <Label Text="Nr telefonu" FontSize="Medium" />
            <Editor Text="{Binding PhoneNumber}" d:Text="123456789" FontSize="Small" Margin="0" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>